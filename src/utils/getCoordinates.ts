// Função para obter coordenadas fixas
export async function getCoordinates(): Promise<{ lat: number; lng: number }> {
    // Coordenadas fixas
    const coordinates = { lat: -12.134700126684958, lng: -44.99033719164091 };
    return coordinates;
}
